#pragma once

#include "LQuark.hpp"
#include "LQBasicCharge.hpp"

class LQGreenCharge : public LQBasicCharge<LQuark> {
public:
    LQuark& child(LQuark& child);
    // LQuark& child(LQuark& child);
    // LQuark& child(LQuark& child);
    // LQuark& child(LQuark& child);
    // LQuark& child(LQuark& child);
    // LQuark& child(LQuark& child);
};
