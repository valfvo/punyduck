1) constructeurs : 
X LQViewable(x, y, bool flex=true)
X LQViewable(x, y, LQNumber width, LQNumber height)
X LQViewable(x, y, LQNumber width, float    height)
X LQViewable(x, y, float    width, LQNumber height)
X LQViewable(x, y, LQImageData& image)



2) classes :
V LQImageData(width, height, format, pixels)

X LQText(string, x, y)

X LQButton(string text, x, y)
X LQButton(x, y)
X LQButton(x, y, width, height)
X LQButton(x, y, width, height, target, callback)
X LQButton(x, y, width, height, callback)



3) Méthodes :
X fitChildrenDimensions();
X displayFlex();
X displayBlock();
X resize(width, height);
V LQ_FOR_EACH(item, callback);
V lqOn("click", callback) -> lqOn<LQEvent>(callback);
V item LQAppModel::getFirst(string model);
X item LQAppModel::getFirst<Type>();
X hide()
X unhide()



4) Structures :
struct Project{LQImage image; LQText titre; LQText desc; GLfloat notes; LQText nomCreateur; LQText prenomCreateur;}
struct Logos {LQImage search; LQImage mosaique; LQImage liste; LQImage personne; LQImage boutonEnvoi;}
struct collectionProject {LQString titre; LQImage image; LQText etat; LQImage etoile;}
struct Profile {LQString nom; LQImage imageProfile; LQImage imageCouverture; }
struct Friend {LQString pseudo; LQImage buttonOffline; LQImage buttonOnline;}
struct Subject{LQString nom;}
